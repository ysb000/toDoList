# To-do_list
* **26/06/23 제작 시작**
* `Header` 영역 HTML 구성 완료
* **26/06/24 기록**
* `Main` HTML 1~3행 작성 
1. 날짜~할일 목록 리스트
2. 작성버튼
3. 작성버튼 클릭시 실행화면(입력화면)
## 추후 동적 업데이트 예정 목록
* **강조**표시는 피그마 디자인 업데이트가 필요합니다.
* 상단로고 클릭 시 메인(홈) 화면으로 이동
* 상단 검색 버튼 클릭시 **검색창 실행** 기능
* 상단 메뉴 클릭시 **메뉴 출력 기능**
* 상단 카테고리 ALL 클릭시 모든 할 일 목록 출력 기능
* 상단 카테고리 Today 클릭시 현재 날짜에 작성한 목록만 출력하는 기능
* ALL과 Today 옆 숫자가 할일 목록 상태에 따라 실시간 변경되는 기능
* 날짜 터치(클릭)시 **날짜 변경 가능**
* 등록된 할 일 목록 터치(클릭)시 완료 또는 **수정, 삭제 기능** 
* 글쓰기 CTA 버튼 터치(클릭)시 글쓰기 팝업 실행되는 기능
* 글쓰기 팝업 내에 원하는 할 일 목록 입력되는 기능
* 글쓰기 팝업 내에 글쓰기 버튼 터치(클릭)시 할 일 목록 추가되는 기능
## CSS 작성 시 주의사항 및 기본순서
* css로 디자인 적용 전 HTML 태그의 고유 디자인이 활성화되어있으면 안된다.
* (위)가 활성화되어있다면 반드시 css 명령어를 통해 html 디자인을 초기화해야한다.
* 초기화(reset.css) 연결 후 `color`, `font-family` 정보 제작하는 디자인 특징에 맞게 재체크하기
* 초기화 연결 시 기존 디자인하는 다른 css 파일보다 반드시 먼저 해결하기(순서상 가장 위로)
* 디자인 css 진행 시 디자인하려는 html의 Body 태그의 첫번째 자식 태그부터 선택자로 만들고 순서대로 진행하기
* 특정 대상에 `width, height` 크기관련 속성 작성 시 영역의 크기와 범위, 위치를 구분하기 위해 `background-color`를 설정하기(실제 디자인 배경색이랑 연관없이 구분을 위한 테스트용으로 주로 영문명 사용)
* **26/06/29**
## css main 1,3번 작성
* **26/06/30**
## css 완성
* 글쓰기 버튼&글쓰기 팝업 고정, 일정 목록 스크롤, 배경색 정리